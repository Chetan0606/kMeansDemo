% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kmeans.R
\name{assign_clusters_interface}
\alias{assign_clusters_interface}
\alias{assign_clusters}
\title{Assign Data Points to Nearest Clusters}
\usage{
assign_clusters(data, n, p, cluster_centers, k)
}
\arguments{
\item{data}{A numeric matrix where each row represents a data point and each column represents a feature.}

\item{n}{An integer specifying the number of data points.}

\item{p}{An integer specifying the number of features or dimensions.}

\item{cluster_centers}{A numeric matrix representing cluster centers.}

\item{k}{An integer specifying the number of clusters.}
}
\value{
A numeric vector of cluster assignments, where each element indicates the cluster to which the corresponding data point belongs.
}
\description{
This function assigns data points to the nearest clusters based on their distances to cluster centers.
}
\examples{
# Example: Assign data points to clusters in 2D space
data <- matrix(c(1.2, 2.3, 3.1, 4.8, 5.7, 6.6), nrow = 2)
n <- nrow(data)
p <- ncol(data)
cluster_centers <- matrix(c(1.0, 2.0, 4.0, 5.0), nrow = 2)
k <- nrow(cluster_centers)
assignments <- assign_clusters(data, n, p, cluster_centers, k)
cat("Cluster Assignments:", assignments, "\n")
}
